{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Etablissement list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nometablissement</th>
                <th>Adresseetablissement</th>
                <th>Teletablissement</th>
                <th>Faxetablissement</th>
                <th>Typeetablissement</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('etablissement_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nomEtablissement }}</td>
                <td>{{ entity.adresseEtablissement }}</td>
                <td>{{ entity.telEtablissement }}</td>
                <td>{{ entity.faxEtablissement }}</td>
                <td>{{ entity.typeEtablissement }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('etablissement_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('etablissement_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('etablissement_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
