{% extends 'AdmiScoBundle::standard_layout.html.twig'%}
{% block breadcrumb %}
    <h3>
        <a href="{{path('welcome')}}">Aller à  l'application</a>
    </h3>
    
    {%endblock %}
    
{% block form %}
    <h1>DashBord</h1>

    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Email</th>
                <th>Nom Utilisateur</th>
                <th>Etat</th>
                <th>Derniere Connection</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.username }}</td>
                <td>{{entity.enabled}}</td>
                <td>{{entity.lastLogin|date('Y-m-d:H:i:s')}}</td>
                <td>
                
                        <a href="{{ path('admin_show', { 'id': entity.id }) }}"><button type='submit' class='btn-primary'>Afficher </button></a>
                    
                        <a href="{{ path('admin_edit', { 'id': entity.id }) }}"><button type='submit' class='btn-primary'>Modifier </button></a>
                   
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        
            <a href="{{ path('admin_new') }}">
                <button type='submit' class='btn-primary'>Ajouter </button>
            </a>
       
    {% endblock %}
