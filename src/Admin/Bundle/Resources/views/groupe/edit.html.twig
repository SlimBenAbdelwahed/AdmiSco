{% extends 'AdmiScoBundle::standard_layout.html.twig'%}

{% block form %}
    <h1>groupe edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('groupe') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
